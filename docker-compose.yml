services:
  app:
    build: .
    container_name: world-cup-sweepstake
    ports:
      - "3001:3001"
    env_file: .env
    environment:
      - PORT=3001
      - DATA_DIR=/data
    volumes:
      - sweepstake_data:/data
    restart: unless-stopped

volumes:
  sweepstake_data:
